Part I

	bzero(ok)
	strcat(ok)
	isalpha(ok)
	isdigit(ok)
	isalnum(ok)
	isascii(ok)
	isprint(ok)
	toupper(ok)
	tolower(ok)
	puts(ok)

	Part II

	strlen(ok)
	memset(ok)
	memcpy(ok)
	strdup(ok)

Part III

cat(ok)

BONUS

ft_strnew(ok)
ft_putchar()
ft_strcmp()
